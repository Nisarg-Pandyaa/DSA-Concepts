# When It Should Click!

Use when we have to count integers in a range `[L,R]` that satisfy a specific property.
Instead of iterating through every number (which is too slow), we build the number digit by digit from `left -> right`.

---

# Concept




# Problems
LC : 233, 1012, 600, 902, 2827
